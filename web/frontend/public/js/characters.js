function init3D(){let c=document.getElementById("maskCanvas");if(!c||!window.THREE)return;let r=new THREE.WebGLRenderer({canvas:c,alpha:true,antialias:true});let s=new THREE.Scene();let cam=new THREE.PerspectiveCamera(50,1,0.1,100);cam.position.z=5;let light=new THREE.PointLight(0xffffff,2);light.position.set(2,2,3);s.add(light);let g=new THREE.SphereGeometry(0.6,24,24);let m1=new THREE.MeshStandardMaterial({color:0x00f0ff,emissive:0x0055ff});let m2=new THREE.MeshStandardMaterial({color:0xff3b7d,emissive:0x550022});let mask=new THREE.Mesh(g,m1);let kus=new THREE.Mesh(g,m2);mask.position.x=-1.1;kus.position.x=1.1;s.add(mask);s.add(kus);function rez(){let w=c.clientWidth||320,h=c.clientHeight||220;r.setSize(w,h,false);cam.aspect=w/h;cam.updateProjectionMatrix()}window.addEventListener("resize",rez);rez();(function anim(){mask.rotation.y+=0.02;kus.rotation.y-=0.02;let b=window.GEMIVAS_BEAT||0;mask.position.y=Math.sin(Date.now()/400)*0.15+b*0.8;kus.position.y=Math.cos(Date.now()/420)*0.15+b*0.8;mask.scale.setScalar(1+b*0.6);kus.scale.setScalar(1+b*0.6);let L=window.GEMIVAS_LIFE||{};let a=L.action||"idle";let k=L.intensity||0.5;if(a=="wiggle"){mask.rotation.z=Math.sin(Date.now()/60)*k*0.8;kus.rotation.z=Math.cos(Date.now()/55)*k*0.8}if(a=="run"){mask.position.x=-1.1+Math.sin(Date.now()/120)*k*2.2;kus.position.x=1.1+Math.cos(Date.now()/120)*k*2.2}if(a=="hide"){mask.position.z=-k*4;kus.position.z=-k*4}r.render(s,cam);requestAnimationFrame(anim)})()}
document.addEventListener("DOMContentLoaded",init3D);
